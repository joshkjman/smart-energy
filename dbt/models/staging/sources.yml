version: 2
sources:
  - name: bronze
    meta:
      external_location: "../data/bronze/{name}/**/*.json"   # {name} → the table name below
    tables:
      - name: demand
      - name: weather_forecast